import 'package:flutter/material.dart';

/// The Todo tab: reserved per the mockup; the body is left empty until the
/// section is implemented (T-UI-10).
final class TodoTab extends StatelessWidget {
  /// Creates the todo tab.
  const TodoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Todo'));
  }
}
